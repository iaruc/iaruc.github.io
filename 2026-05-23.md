**最近整理了之前机器学习（基础）的笔记：**



# 机器学习



## 一、四种训练方式


根据数据和目标的不同，主要分为四类：

**监督学习**是最常见的方式——给模型输入数据，同时告诉它正确答案（标签），让它学会"给定输入，预测输出"。但这类方法面临三个核心挑战：标注成本高、容易过拟合、样本往往不均衡。

**无监督学习**则没有标签，典型任务是聚类。虽然没有"标准答案"，但模型仍然有目标——用损失函数来衡量好坏，模型自己去发现数据中的结构。

**在线学习**强调实时性，模型能够随着新数据的到来不断更新，适应变化的环境。

**强化学习**最为特殊。它追求的是长期效益最大化，并且模型的行为会影响环境本身。其核心机制是：
- 当前状态 sts_t st​，执行动作 ata_t at​
- 环境反馈奖励 rtr_t rt​ 并进入新状态 st+1s_{t+1} st+1​
- 模型通过不断试错，学出一个策略：在状态 ss s 下选择各动作的概率分布
强化学习的难点在于奖励信号往往稀疏或延迟，模型无法立刻知道自己做对了没有。


## 二、神经网络家族

**基础结构**：神经网络

**骨架**：输入层 → 隐藏层（可多个）→ 输出层。每一个节点的计算分两步：先做加权求和（线性变换），再通过激活函数引入非线性，从而让网络具备拟合复杂函数的能力。

**训练靠反向传播**：计算损失对每个参数的梯度，然后用优化算法（最常见的是梯度下降）更新权重。

### 多层感知机（MLP）

MLP 是"标准形态"的神经网络，在输入层和输出层之间加入若干隐藏层。常见激活函数有三种：ReLU、Sigmoid、Tanh。

MLP 最适合处理表格数据或特征向量，但对图像、文本等有空间/序列结构的数据，它并不擅长。

### 卷积神经网络（CNN）

CNN 为图像而生，有三层结构：
**- 卷积层**：用卷积核提取局部特征（边缘、纹理……）
**- 池化层**：压缩特征图，保留最重要的信息，减少计算量
**- 全连接层**：汇聚特征，输出最终分类结果

### 循环神经网络（RNN）

RNN 的核心思想是记忆。它把上一个时间步的输出作为下一个时间步的输入，天然支持序列数据（文本、时间序列）。但它有几个痛点：
- 无法并行训练，速度慢
- 记忆力有限，长距离依赖容易丢失
- 对超参数敏感，训练难收敛，容易过拟合或震荡

RNN 的输出层通常接 Softmax 激活函数，损失函数用交叉熵，且各时间步共享同一组参数。

为了克服"遗忘"的问题，**长短期记忆网络**（LSTM）在 RNN 基础上引入了三个门控机制：遗忘门（决定丢掉哪些历史信息）、输入门（决定吸收哪些新信息）、输出门（决定输出哪部分状态）。

### Transformer

Transformer 彻底抛弃了时间顺序的假设，它的核心机制是自注意力：
- 每一个词都可以直接关注序列中的任何其他词，通过计算注意力得分来加权聚合信息。
  
这带来两个优势：
- 可以并行训练，效率远超 RNN
- 多头注意力机制让模型从多个角度同时理解序列
  
代价：
- 由于丢弃了时间顺序，需要额外加入位置编码来告诉模型每个词的位置。


## 三、模型训练的通用套路

无论哪种模型，训练流程都遵循同一套框架：

**1. 目标函数（损失函数）**：衡量模型预测与真实值的差距。

任务类型	损失函数
线性回归	均方误差（MSE）
多分类（单标签）	交叉熵损失 + Softmax
多分类（多标签）	二元交叉熵 + Sigmoid

**2. 优化算法**：梯度下降是最核心的优化思路——沿着损失函数下降最快的方向更新参数，直到收敛。

**3. 数据集划分**：
**- 训练集**：用来生成模型
**- 验证集**：用来选择模型、防止过拟合（性能不再提升时停止训练）
**- 测试集**：最终评估模型的真实性能

防止过拟合还可以：对数据做随机扰动/数据增强、加正则化等。

**4. 数据质量**：这一点常被忽视，却极为关键。标签错误、特征冗余、样本偏差，任何一个问题都会严重影响模型效果。
